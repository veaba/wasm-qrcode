{
  "name": "@veaba/qrcode",
  "version": "0.0.2",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "rsw": "rsw",
    "watch": "yarn rsw watch",
    "test": "node scripts/test.js",
    "test:unit": "vitest run",
    "test:browser": "vitest run --browser",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:shared": "node scripts/test.js --shared",
    "test:node": "node scripts/test.js --node",
    "test:ts": "node scripts/test.js --ts",
    "benchmark": "node scripts/benchmark.js",
    "benchmark:backend": "node scripts/benchmark.js",
    "benchmark:node": "node scripts/benchmark.js --node",
    "benchmark:bun": "node scripts/benchmark.js --bun",
    "benchmark:rust": "node scripts/benchmark.js --rust",
    "benchmark:clean": "node scripts/benchmark-clean.js",
    "publish:check": "node scripts/publish-check.js",
    "publish:all": "node scripts/publish.js",
    "publish:dry-run": "node scripts/publish.js --dry-run",
    "publish:help": "echo '查看 PUBLISH.md 获取详细发包指南'",
    "docs:dev": "rspress dev",
    "docs:dev1": "cd docs && rspress dev",
    "docs:build": "cd docs && rspress build",
    "docs:preview": "cd docs && rspress preview"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "@vitest/browser": "^2.1.0",
    "@vitest/coverage-v8": "^2.1.0",
    "playwright": "^1.49.0",
    "typescript": "^4.8.4",
    "vite": "^3.1.8",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "rspress": "^1.47.1"
  }
}