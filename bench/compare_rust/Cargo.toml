[package]
name = "svg_compare"
version = "0.1.0"
edition = "2021"

[dependencies]
# 我们的 QRCode 库（本地路径）
qrcode-rust = { path = "../packages/qrcode-rust" }

# kennytm 的 QRCode 库（crates.io）
qrcode = "0.14"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "svg_benchmark"
harness = false
