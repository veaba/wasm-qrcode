# SVG QRCode 生成性能基准测试

## 测试概述

本基准测试对比了以下 Rust QRCode 生成库的 SVG 生成性能：

| 包 | 描述 | 状态 |
|---|---|---|
| `kennytm-qrcode` | 社区流行的 Rust QRCode 库（参考基准） | ✅ 验证通过 |
| `@veaba/qrcode-rust` | 纯 Rust 实现，参考版本 | ⚠️ 部分验证失败 |
| `@veaba/qrcode-fast` | 极致性能优化版本 | ⚠️ 部分验证失败 |

## 最新测试结果

*最后更新: 2026-02-02*

### 测试环境

- **平台**: Windows x64
- **Rust**: rustc 1.89.0
- **测试时间**: 2026-02-02
- **验证工具**: resvg + rqrr

### 测试用例

| 名称 | 文本内容 | 版本 | 模块数 |
|------|---------|------|--------|
| Simple | "Hello World" | 1 | 21x21 |
| Complex | "Test QR Code 123" | 3 | 25x25 |
| URL | "https://github.com/veaba/qrcodes" | 3 | 29x29 |
| Long | "Email: test@example.com \| Phone: +1-234-567-8900..." | 6 | 37x37 |

### 性能对比

| 测试用例 | kennytm (µs) | qrcode-rust (µs) | qrcode-fast (µs) | fast 加速比 |
|---------|-------------|-----------------|-----------------|------------|
| Simple | ~230 | ~32 | ~11 | **~21x** |
| Complex | ~350 | ~37 | ~17 | **~21x** |
| URL | ~470 | ~55 | ~23 | **~20x** |
| Long | ~870 | ~90 | ~40 | **~22x** |

### 验证状态

| 测试用例 | kennytm | qrcode-rust | qrcode-fast |
|---------|---------|-------------|-------------|
| Simple | ✅ | ✅ | ✅ |
| Complex | ✅ | ❌ | ❌ |
| URL | ✅ | ✅ | ✅ |
| Long | ✅ | ❌ | ❌ |

**说明**: 
- ✅ 表示生成的二维码可以通过标准扫描器正确解码
- ❌ 表示验证失败（Reed-Solomon 纠错码错误）

## 主要发现

### 🏆 性能冠军

`@veaba/qrcode-fast` 在所有测试用例中表现最佳：

- 比 `kennytm-qrcode` 快 **20-22 倍**
- 比 `@veaba/qrcode-rust` 快 **2-3 倍**

### ⚠️ 验证问题

`@veaba/qrcode-rust` 和 `@veaba/qrcode-fast` 在以下情况验证失败：
- 版本 3+ 的复杂文本（如 "Test QR Code 123"）
- 版本 6 的长文本

这表明 Reed-Solomon 纠错码计算在某些情况下存在问题，需要进一步修复。

### 📊 SVG 文件大小

| 包 | Simple | Complex | URL | Long |
|---|--------|---------|-----|------|
| kennytm | ~4.2 KB | ~6.0 KB | ~8.3 KB | ~13.4 KB |
| qrcode-rust | ~13.5 KB | ~18.4 KB | ~25.6 KB | ~40.1 KB |
| qrcode-fast | ~4.1 KB | ~5.6 KB | ~6.9 KB | ~10.7 KB |

`qrcode-fast` 生成的 SVG 文件大小与 `kennytm-qrcode` 相当，而 `qrcode-rust` 的文件较大（因为使用了不同的 SVG 生成方式）。

## 运行基准测试

### 快速测试（10次运行）

```bash
pnpm bench:svg:rust:quick
```

### 完整测试（100次运行）

```bash
pnpm bench:svg:rust
```

### 命令说明

```bash
# 仅运行 Rust 测试
pnpm bench:svg:rust:quick    # 快速模式
pnpm bench:svg:rust          # 完整模式

# 运行所有测试（包括 JS，如果有兼容性问题会跳过）
pnpm bench:svg:quick
pnpm bench:svg
```

## 结果文件

测试完成后，结果会保存到以下位置：

- **SVG 文件**: `docs/bench/benchmark-output/*.svg`
- **JSON 报告**: `docs/public/benchmark_svg_rust.json`

## 查看详细结果

<iframe src="/benchmark_svg_rust.json" width="100%" height="400px" style={{border: '1px solid #ddd', borderRadius: '4px'}} />

## 使用建议

| 场景 | 推荐 | 原因 |
|------|------|------|
| 极致性能 | `@veaba/qrcode-fast` | 最快的生成速度 |
| 生产环境 | `kennytm-qrcode` | 稳定可靠，验证通过 |
| 开发测试 | `@veaba/qrcode-rust` | 功能完整，便于调试 |

## 已知问题

1. **Reed-Solomon 纠错码**: `@veaba` 包在某些复杂文本上验证失败
2. **Windows ESM**: JavaScript 包测试在 Windows 上有兼容性问题，默认只运行 Rust 测试

## 修复计划

- [ ] 修复 Reed-Solomon 纠错码计算
- [ ] 统一 GF(256) 表初始化算法
- [ ] 添加更多边界测试用例

---

*数据自动生成的基准测试报告*
