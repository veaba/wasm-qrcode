[package]
name = "qrcode-fast"
version = "0.0.1-alpha"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "comparison_bench"
harness = false
